#The goDepot class can read files.
#the entries look like this and describe stock papers:

ticker		= (symbol)
name		= (name)
nr		= (nr)
amount		= (how many)
buyprice	= (price)
sellprice	= (sell price)
costs		= (other costs)
selldate	= (dd.mm.yyyy)
buydate		= (dd.mm.yyyy)
sold		= (0 or 1)
currency	= (DM, $, USD, whatever...)
