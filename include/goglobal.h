#ifndef GOGLOBAL_H
#define GOGLOBAL_H

namespace goGlobal
{
    extern bool ILInitialized;       // Indicates if DevIL is
                                     // initialized (if it is 
                                     // used at all).
}

#endif
